{
  "name": "omnifocus-plugins",
  "version": "1.0.0",
  "description": "TypeScript OmniFocus plugins",
  "scripts": {
    "build": "tsc && node scripts/post-build.js",
    "clean": "rimraf *.omnijs"
  },
  "devDependencies": {
    "rimraf": "^6.1.0",
    "typescript": "^5.3.3"
  }
} 